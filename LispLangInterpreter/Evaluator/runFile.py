
#load library files into library tree
library = None

#load initial file with file runner function
def runFile(pathElements: [str]):
    #load file
    #put file location into stackframe, demacro and run
    #once finished, save final return values, key value pair are exports
    pass